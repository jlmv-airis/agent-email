{
  "name": "API - Cambiar Estado de Cuenta",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "estado-cuenta",
        "responseMode": "lastNode",
        "options": {}
      },
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [0, 0],
      "id": "webhook-estado",
      "name": "Webhook Estado"
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "=UPDATE cuentas_email SET activo = {{ $json.body.activo }} WHERE email = '{{ $json.body.email }}' RETURNING *;"
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [250, 0],
      "id": "db-update-status",
      "name": "Actualizar Estado",
      "credentials": {
        "postgres": {
          "id": "CREDENCIAL_POSTGRES_DOCKER",
          "name": "Postgres Local (Docker)"
        }
      }
    }
  ],
  "connections": {
    "Webhook Estado": {
      "main": [[{"node": "Actualizar Estado", "type": "main", "index": 0}]]
    }
  }
}