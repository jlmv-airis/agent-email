# 📋 Plan de Desarrollo - v1.1.20

**Rama activa:** `v1.1.20`  
**Última actualización:** 6 de mayo de 2026  
**Estado:** ✅ Completado

---

## 🎯 Objetivos de la Iteración v1.1.20

- [x] Cambiar panel "Tendencia de Tickets" por "Email Enviados y recibidos, por operador"
- [x] Implementar lógica en el frontend para mostrar estadísticas por operador
- [x] Asegurar que el gráfico refleje los datos de envíos y recepciones

---

## 🚀 Implementado en v1.1.20

### Infraestructura
- [x] Rama v1.1.20 definida
- [x] Versión actualizada en footer (v1.1.20)
- [x] CONTEXT.md actualizado a v1.1.20

### Dashboard UI & Analítica
- [x] Título del panel actualizado a "Emails" (más conciso).
- [x] Gráfico de tendencias convertido a gráfico de barras horizontales (mejor alineación de nombres).
- [x] Implementación de botones de filtro (Todos, Recibidos, Enviados) para el gráfico.
- [x] Nuevo endpoint `/api/analytics/operators` en el backend.
- [x] Registro del operador en correos enviados (columna `asignado_a` en `hilos`).
- [x] Corrección de bug de placeholders en inserción de correos enviados.

---

## 📊 Cambios Técnicos Realizados

### Backend (`backend/server.py`)
- Nuevo endpoint `/api/analytics/operators` que calcula métricas de enviados/recibidos.
- Ajuste en `send_email` para registrar el operador y arreglar error SQL.

### Frontend (`frontend/index.html`)
- Título simplificado a "Emails".
- Configuración de `Chart.js` optimizada con `indexAxis: 'y'` y barras horizontales.
- Agregados botones de filtrado dinámico con lógica en JS (`setChartFilter`).
- Función `updateCharts` sincronizada con el nuevo endpoint y lógica de ocultación de datasets.

---

## 🧪 Testing

### Casos de Test
- [x] Verificar que el título del panel sea correcto.
- [x] Verificar que el gráfico cargue sin errores.
- [x] Validar que los datos mostrados correspondan a la actividad de los operadores.

---

**Rama anterior:** `v1.1` → **Rama actual:** `v1.1.20` → **Próxima rama:** `v1.1.21`
